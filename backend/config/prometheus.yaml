global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: zpay-backend
    static_configs:
      - targets:
          - host.docker.internal:8000
        labels:
          service: zpay-backend
          env: local