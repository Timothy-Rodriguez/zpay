package constants

const (
	JwtToken            = "jwt-token"
	RefrestToken        = "refresh-token"
	Claims              = "claims"
	ClaimsEmail         = "claims-email"
	AuthorizationHeader = "Authorization"
)
