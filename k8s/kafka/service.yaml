apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: prod
spec:
  selector:
    app: kafka
  ports:
    - port: 9092
      targetPort: 9092
      name: plaintext
  type: ClusterIP
